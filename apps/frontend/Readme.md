# the filter option for monitor status

## "Smart Fetch / Cache" strategy. 
It perfectly balances frontend speed (instant swapping between 1H, 24H, and 7D without loading spinners) and backend performance (not fetching massive 30D/90D datasets unless the user explicitly asks for them).